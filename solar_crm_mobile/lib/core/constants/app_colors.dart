import 'package:flutter/material.dart';

class AppColors {
  // Google / Material 3 Clean Palette
  static const Color primary = Color(0xFF1A73E8); // Google Blue
  static const Color primaryDark = Color(0xFF0D47A1); // Deep Slate Blue
  static const Color primarySoft = Color(0xFFE8F0FE); // Soft Google Tint

  // Background & Surface
  static const Color bg = Color(0xFFF8F9FA); // Material Gray 50
  static const Color card = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF1F3F4);
  static const Color hover = Color(0xFFE8EAED);

  // Borders & Dividers
  static const Color border = Color(0xFFDADCE0);
  static const Color borderStrong = Color(0xFFBDC1C6);

  // Status Alerts
  static const Color success = Color(0xFF137333); // Google Green
  static const Color successSoft = Color(0xFFE6F4EA);
  static const Color danger = Color(0xFFD93025); // Google Red
  static const Color dangerSoft = Color(0xFFFCE8E6);
  static const Color warning = Color(0xFFF9AB00); // Google Yellow
  static const Color warningSoft = Color(0xFFFEF7E0);
  static const Color info = Color(0xFF129EAF);
  static const Color infoSoft = Color(0xFFE4F4F8);

  // Accents & Roles
  static const Color purple = Color(0xFF7C3AED); // Accent Purple for Reassign
  static const Color purpleSoft = Color(0xFFEDE9FE);

  // Typography
  static const Color textPrimary = Color(0xFF202124); // Dark Charcoal
  static const Color textSecondary = Color(0xFF5F6368);
  static const Color textMuted = Color(0xFF80868B);
}
